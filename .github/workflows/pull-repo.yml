name: PullRepo

on:
  schedule:
    - cron:  '* * * * *'

  workflow_dispatch:

jobs:
  build:
    runs-on: [self-hosted]

    steps:
      - name: Pull /home/ubuntu/repo
        run: cd /home/ubuntu/repo && git pull
