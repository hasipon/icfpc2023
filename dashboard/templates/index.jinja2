{% extends "base.jinja2" %}
{% block content %}
    <div class="container-fluid">
        <h2>問題一覧 ({{ problems|length }} 件)</h2>
    {#
        <dl>
            <dt>Solved</dt>
            <dd>{{ result_by_api.solved_problem_count }} / {{ problems|length }}</dd>
            <dt>TotalCost</dt>
            <dd>{{ result_by_api.total_cost }}</dd>
        </dl>
    #}
        <br>
        <table class="table">
            <tr>
                <th class="text-center" scope="col">Problem</th>
                <th class="text-center" scope="col">Image</th>
                <th class="text-center" scope="col">Solutions 1st</th>
                <th class="text-center" scope="col">Solutions 2nd</th>
            </tr>

            {% for p in problems.values() %}
                <tr>
                    <td class="text-center" id="{{ p.id }}">
                        <a href="http://35.221.99.118/repo/problem.json/{{ p.id }}.json">{{ p.name }}</a>
                        <br>
                        <a href="/solutions/{{ p.id }}">回答一覧 ({{solutions[p.id] | length }} 件)</a><br>
                        <br>
                        <span style="font-weight: bold">Room&Stage</span><br>
                        {{ p.room_width }} x {{ p.room_height }} <br>
                        {{ p.stage_width }} x {{ p.stage_height }} <br>
                        {{ p.stage_bottom_left }}<br>
                        <br>
                        <span style="font-weight: bold">Musicians&Attendees</span><br>
                        {{ p.musicians_size }} <br>
                        {{ p.attendees_size }}
                    </td>

                    <td class="text-center">
                        <a href="{{ p.svg_path }}">
                            <img width="300" height="300" src="{{ p.svg_path }}">
                        </a>
                    </td>

                    <td class="text-center">
                        {% if 0 < solutions[p.id] | length %}
                            {{ solutions[p.id][0].name }}<br>
                            {{ solutions[p.id][0].score }} <br>
                            <a href="{{ solutions[p.id][0].svg_path }}">
                                <img width="300" height="300" src="{{ solutions[p.id][0].svg_path }}">
                            </a>
                        {% else %}
                            <p>No submission</p>
                        {% endif %}
                    </td>
                    <td class="text-center">
                        {% if 1 < solutions[p.id] | length %}
                            {{ solutions[p.id][1].name }}<br>
                            {{ solutions[p.id][1].score }}<br>
                            <a href="{{ solutions[p.id][1].svg_path }}">
                                <img width="300" height="300" src="{{ solutions[p.id][1].svg_path }}">
                            </a>
                        {% else %}
                            <p>No submission</p>
                        {% endif %}
                    </td>
                </tr>
            {% endfor %}
        </table>
    </div>

{% endblock %}

